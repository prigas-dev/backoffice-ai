{
  "$schema": "../../schemas/operation_specification_schema.json",
  "parameters": [
    {
      "type": "string",
      "name": "userName"
    }
  ],
  "actions": [
    {
      "type": "sqlquery",
      "query": "SELECT * FROM FROM tasks t INNER JOIN user_tasks ut ON t.id = ut.task_id INNER JOIN users u ON ut.user_id = u.id WHERE u.name = ? AND t.deleted_at IS NULL ORDER BY t.due_date ASC"
    }
  ]
}
